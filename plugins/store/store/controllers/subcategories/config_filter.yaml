scopes:
    created_at:
        label: 'تاريخ الإنشاء'
        type: date
        conditions: created_at >= ':filtered'
    category:
        label: 'التصنيفات'
        modelClass: Store\Store\Models\Category
        conditions: category_id in (:filtered)
        nameFrom: name